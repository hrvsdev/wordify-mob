export default function Loader() {
  const styles = {
    display: "flex",
    justifyContent: "center",
    height: "100%",
    alignItems: "center",
  };

  return <h1 style={styles}>Loading</h1>;
}
